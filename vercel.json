{
  "buildCommand": "pnpm turbo build",
  "ignoreCommand": "pnpm dlx turbo-ignore",
  "edge-config": {
    "runtimeConfig": {
      "contractAddress": {
        "type": "string"
      },
      "theme": {
        "type": "string",
        "enum": ["light", "dark"]
      },
      "notifications": {
        "type": "boolean"
      }
    }
  }
}
